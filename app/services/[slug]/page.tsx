import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { ArrowDown, ArrowRight, Check, CircleCheckBig, Layers3 } from "lucide-react";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";
import { LeadForm } from "@/components/forms/lead-form";
import { JsonLd } from "@/components/seo/json-ld";
import { ButtonLink } from "@/components/ui/button";
import { getService, services } from "@/data/services";
import { pageMetadata } from "@/lib/metadata";
import { absoluteUrl } from "@/lib/utils";

export function generateStaticParams(){return services.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const service=getService((await params).slug);return service?pageMetadata(service.title,service.description,`/services/${service.slug}`):{};}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const service=getService((await params).slug);if(!service)notFound();const schema=[{"@context":"https://schema.org","@type":"Service",name:service.name,serviceType:service.name,description:service.description,url:absoluteUrl(`/services/${service.slug}`),provider:{"@type":"Organization",name:"AI Global",url:absoluteUrl()}},{"@context":"https://schema.org","@type":"FAQPage",mainEntity:service.faq.map(item=>({"@type":"Question",name:item.question,acceptedAnswer:{"@type":"Answer",text:item.answer}}))}];return <><JsonLd data={schema}/><section className="relative overflow-hidden bg-[var(--ocean)] py-14 text-white md:py-24"><div className="absolute right-[-12%] top-[-40%] size-[620px] rounded-full border border-[var(--brand)]/15"/><div className="absolute right-[2%] top-[-20%] size-[420px] rounded-full border border-white/[.06]"/><div className="container-main relative"><Breadcrumbs light items={[{label:"企业服务",href:"/services"},{label:service.name}]}/><div className="mt-12 grid gap-12 lg:grid-cols-[1.05fr_.65fr] lg:items-end"><div><p className="font-mono text-[10px] font-bold uppercase tracking-[.22em] text-[var(--brand)]">{service.eyebrow}</p><h1 className="mt-6 max-w-4xl text-balance font-display text-5xl font-semibold leading-[1.02] tracking-[-.055em] md:text-7xl">{service.title}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-white/58">{service.description}</p><div className="mt-9 flex flex-wrap gap-3"><ButtonLink href="#inquiry" size="lg">获取项目方案<ArrowRight className="size-4"/></ButtonLink><ButtonLink href="#deliverables" size="lg" variant="outline" className="border-white/18 text-white hover:bg-white/8">查看交付内容<ArrowDown className="size-4"/></ButtonLink></div></div><div className="rounded-[26px] border border-white/10 bg-white/[.04] p-6 backdrop-blur"><p className="font-mono text-[10px] uppercase tracking-[.18em] text-white/36">Suitable for</p><div className="mt-5 flex flex-wrap gap-2">{service.audiences.map(item=><span key={item} className="rounded-full border border-white/12 px-3 py-2 text-xs text-white/65">{item}</span>)}</div><p className="mt-7 border-t border-white/10 pt-5 text-xs leading-6 text-white/40">不承诺确定排名、确定ROI或未经验证的增长结果。所有项目基于可审计范围、数据和里程碑推进。</p></div></div></div></section><section id="deliverables" className="section-pad bg-white"><div className="container-main"><div className="grid gap-12 lg:grid-cols-[.75fr_1.25fr]"><div><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand-dark)]">What we deliver</p><h2 className="mt-5 font-display text-4xl font-semibold tracking-[-.04em] md:text-5xl">把策略变成<br/>可以运营的资产</h2><p className="mt-5 text-sm leading-7 text-[var(--muted)]">交付范围会根据诊断调整，但始终包含明确的业务上下文、执行责任和效果验证方式。</p></div><div className="grid gap-3 sm:grid-cols-2">{service.deliverables.map((item,index)=><div key={item} className="flex min-h-28 items-start gap-4 rounded-2xl border border-[var(--line)] bg-[var(--paper)] p-5"><span className="flex size-7 shrink-0 items-center justify-center rounded-full bg-[var(--brand)] font-mono text-[10px] font-bold">{String(index+1).padStart(2,"0")}</span><p className="text-sm font-semibold leading-6">{item}</p></div>)}</div></div></div></section><section className="section-pad soft-grid bg-[var(--paper)]"><div className="container-main"><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand-dark)]">Delivery Process</p><h2 className="mt-5 font-display text-4xl font-semibold tracking-[-.04em] md:text-5xl">从诊断到持续增长</h2><div className="mt-10 grid gap-3 md:grid-cols-3 lg:grid-cols-6">{service.process.map((step,index)=><div key={step} className="relative rounded-[20px] border border-[var(--line)] bg-white p-5"><span className="font-mono text-[10px] text-[var(--brand-dark)]">0{index+1}</span><h3 className="mt-8 font-semibold">{step}</h3>{index<service.process.length-1&&<ArrowRight className="absolute -right-3 top-1/2 z-10 hidden size-5 rounded-full bg-[var(--paper)] p-1 text-[var(--muted)] lg:block"/>}</div>)}</div></div></section><section className="section-pad bg-[var(--ocean)] text-white"><div className="container-main"><div className="grid gap-12 lg:grid-cols-[.7fr_1.3fr]"><div><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand)]">Engagement</p><h2 className="mt-5 font-display text-4xl font-semibold tracking-[-.04em] md:text-5xl">合作方式</h2><p className="mt-5 text-sm leading-7 text-white/50">首期不公开固定价格。诊断后根据市场、范围、内容与技术复杂度提供项目制报价。</p></div><div className="grid gap-4 md:grid-cols-3">{service.packages.map((item,index)=><div key={item.name} className="rounded-[24px] border border-white/10 bg-white/[.04] p-6"><Layers3 className={`size-5 ${index===1?"text-[var(--orange)]":"text-[var(--brand)]"}`}/><h3 className="mt-7 font-display text-2xl font-semibold">{item.name}</h3><p className="mt-3 text-sm leading-6 text-white/52">{item.description}</p><div className="mt-7 flex items-center gap-2 text-xs text-white/40"><CircleCheckBig className="size-4 text-[var(--brand)]"/>项目制报价</div></div>)}</div></div></div></section><section className="section-pad bg-white"><div className="container-reading"><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand-dark)]">FAQ</p><h2 className="mt-5 font-display text-4xl font-semibold tracking-[-.04em]">常见问题</h2><div className="mt-8 divide-y divide-[var(--line)] border-y border-[var(--line)]">{service.faq.map(item=><details key={item.question} className="group py-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 font-semibold">{item.question}<span className="text-[var(--brand-dark)] group-open:rotate-45">＋</span></summary><p className="max-w-2xl pt-3 text-sm leading-7 text-[var(--muted)]">{item.answer}</p></details>)}</div></div></section><section id="inquiry" className="section-pad bg-[var(--paper)]"><div className="container-main grid gap-12 lg:grid-cols-[.72fr_1.28fr]"><div><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand-dark)]">Talk to AI Global</p><h2 className="mt-5 font-display text-4xl font-semibold tracking-[-.04em] md:text-5xl">获取{service.name}方案</h2><p className="mt-5 text-sm leading-7 text-[var(--muted)]">告诉我们现状、目标市场和核心问题。我们会先判断是否适合、优先级以及下一步需要哪些信息。</p><ul className="mt-7 space-y-3 text-sm">{["1个工作日内确认需求","首次沟通聚焦问题诊断","范围与报价清晰透明"].map(x=><li key={x} className="flex gap-2"><Check className="size-4 text-[var(--brand-dark)]"/>{x}</li>)}</ul></div><div className="rounded-[28px] border border-[var(--line)] bg-white p-6 md:p-9"><LeadForm defaultService={service.slug}/></div></div></section></>}
