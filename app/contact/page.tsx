import type { Metadata } from "next";
import { Clock3, Mail, MessageCircleMore } from "lucide-react";
import { LeadForm } from "@/components/forms/lead-form";
import { ListingHero } from "@/components/content/listing-hero";
import { siteConfig } from "@/data/site-config";
import { pageMetadata } from "@/lib/metadata";
export const metadata:Metadata=pageMetadata("获取AI电商出海方案","提交跨境电商需求，获取增长诊断、独立站、商品内容、广告、SEO/GEO和自动化方案。","/contact");
export default async function ContactPage({searchParams}:{searchParams:Promise<{service?:string}>}){const {service=""}=await searchParams;return <><ListingHero eyebrow="Commerce Growth Inquiry" title="获取AI电商出海方案" description="告诉我们商品、渠道、目标市场和当前最重要的经营问题。AI Global会先完成需求判断，再建议适合的诊断、项目或长期合作方式。"/><section className="section-pad bg-[var(--paper)]"><div className="container-main grid gap-10 lg:grid-cols-[.65fr_1.35fr]"><aside><p className="font-display text-3xl font-semibold">一次有效沟通，从经营上下文开始。</p><p className="mt-4 text-sm leading-7 text-[var(--muted)]">无需准备完整Brief。说明主营商品、现有渠道、目标市场、核心问题和计划时间即可。</p><div className="mt-8 space-y-3">{[{icon:Clock3,label:"响应时间",value:"通常1个工作日内确认"},{icon:Mail,label:"联系邮箱",value:siteConfig.email},{icon:MessageCircleMore,label:"微信",value:siteConfig.wechat}].map(i=>{const Icon=i.icon;return <div key={i.label} className="flex gap-4 rounded-2xl border border-[var(--line)] bg-white p-5"><Icon className="size-5 text-[var(--brand-dark)]"/><div><p className="text-xs text-[var(--muted)]">{i.label}</p><p className="mt-1 text-sm font-semibold">{i.value}</p></div></div>})}</div><p className="mt-6 text-xs leading-6 text-[var(--muted)]">我们不会出售你的联系信息。请勿在首次表单中提交密码、支付信息或未公开的敏感数据。</p></aside><div className="rounded-[28px] border border-[var(--line)] bg-white p-6 shadow-[0_30px_70px_rgba(18,33,43,.06)] md:p-10"><LeadForm defaultService={service}/></div></div></section></>}
