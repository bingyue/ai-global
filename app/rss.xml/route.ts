import { news } from "@/data/news";
import { absoluteUrl } from "@/lib/utils";
const escapeXml=(value:string)=>value.replace(/[<>&'\"]/g,char=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[char]??char));
export async function GET(){const items=news.slice(0,20).map(i=>`<item><title>${escapeXml(i.title)}</title><link>${absoluteUrl(`/news/${i.slug}`)}</link><guid isPermaLink="true">${absoluteUrl(`/news/${i.slug}`)}</guid><description>${escapeXml(i.summary)}</description><category>${escapeXml(i.category)}</category><pubDate>${new Date(i.publishedAt).toUTCString()}</pubDate></item>`).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>AI Global｜AI出海研究院</title><link>${absoluteUrl()}</link><description>AI与全球增长资讯</description><language>zh-CN</language>${items}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, s-maxage=3600, stale-while-revalidate=86400"}})}
