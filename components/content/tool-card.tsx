import Link from "next/link";
import { ArrowUpRight, Languages, Sparkles } from "lucide-react";
import type { ToolItem } from "@/types";

export function ToolCard({tool}:{tool:ToolItem}){return <Link href={`/tools/${tool.slug}`} className="group flex min-h-[270px] flex-col rounded-[24px] border border-[var(--line)] bg-white p-6 transition hover:-translate-y-0.5 hover:border-[var(--brand)]"><div className="flex items-start justify-between"><span className="flex size-12 items-center justify-center rounded-2xl bg-[var(--ink)] text-sm font-black text-white">{tool.name.slice(0,2)}</span>{tool.editorPick&&<span className="flex items-center gap-1 text-[9px] font-bold text-[#ce5a10]"><Sparkles className="size-3"/>编辑推荐</span>}</div><h2 className="mt-6 text-xl font-bold">{tool.name}</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{tool.description}</p><div className="mt-auto flex items-end justify-between pt-6"><div className="space-y-1 text-[10px] text-[#83908b]"><p>{tool.category} · {tool.pricing}</p><p className="flex items-center gap-1"><Languages className="size-3"/>中文：{tool.chineseSupport}</p></div><ArrowUpRight className="size-4 transition group-hover:rotate-45 group-hover:text-[var(--brand-dark)]"/></div></Link>}
