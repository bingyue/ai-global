import { Breadcrumbs } from "@/components/layout/breadcrumbs";

export function ListingHero({ eyebrow, title, description, stats }: { eyebrow: string; title: string; description: string; stats?: { value: string; label: string }[] }) {
  return <section className="relative overflow-hidden bg-[var(--ocean)] py-14 text-white md:py-20"><div className="absolute right-[-8%] top-[-45%] size-[520px] rounded-full border border-[var(--brand)]/12"/><div className="absolute right-[4%] top-[-25%] size-[340px] rounded-full border border-white/6"/><div className="container-main relative"><Breadcrumbs light items={[{label:title}]} /><p className="mt-10 font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[var(--brand)]">{eyebrow}</p><div className="mt-5 grid gap-7 lg:grid-cols-[1fr_.7fr] lg:items-end"><h1 className="max-w-4xl text-balance font-display text-4xl font-semibold leading-[1.04] tracking-[-.045em] md:text-7xl">{title}</h1><p className="max-w-xl text-base leading-8 text-white/58">{description}</p></div>{stats && <div className="mt-12 flex flex-wrap gap-8 border-t border-white/10 pt-6">{stats.map((stat)=><div key={stat.label}><strong className="font-display text-2xl text-white">{stat.value}</strong><span className="ml-2 text-xs text-white/40">{stat.label}</span></div>)}</div>}</div></section>;
}
